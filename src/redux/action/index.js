// Add Item to Cart 
export const addCart = (product) => {
    return {
        type: "ADDCART",
        payload: product
    }
};

// Delete Item from Cart 
export const delCart = (product) => {
    return {
        type: "ADDCART",
        payload: product
    }
};